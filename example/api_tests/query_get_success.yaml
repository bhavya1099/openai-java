# ********RoostGPT********

# Test generated by RoostGPT for test advancedReportTest using AI Type  and AI Model 
# 
# Test generated for /query_get for http method type GET in artillery framework
# RoostTestHash=ccd1119a9e
# 
# 

# ********RoostGPT********
config:
  target: "{{$processEnvironment.opiu_API_HOST}}"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Warm up phase
  payload:
    path: "query_get_success.csv"
    fields:
      - "symbol"
      - "apikey"
      - "function"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: Query API Test Scenario
    flow:
      - log: "Starting Query API Test Scenario"
      - get:
          url: "/query"
          qs:
            symbol: "{{ symbol }}"
            apikey: "{{ apikey }}"
            function: "{{ function }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "Meta Data"
            - hasProperty: "Time Series (Daily)"
      - think: 2
      - log: "Query API request completed successfully"

