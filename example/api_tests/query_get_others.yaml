# ********RoostGPT********

# Test generated by RoostGPT for test advancedReportTest using AI Type  and AI Model 
# 
# Test generated for /query_get for http method type GET in artillery framework
# RoostTestHash=ccd1119a9e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.iopu_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Warm up phase
  payload:
    path: "query_get_others.csv"
    fields:
      - "symbol"
      - "function"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: Query API Test Scenario
    flow:
      - log: "Starting API query test"
      - get:
          url: "/query"
          qs:
            symbol: "{{ symbol }}"
            function: "{{ function }}"
            apikey: "{{ $processEnvironment.API_KEY }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.iopu_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "Meta Data"
            - hasProperty: "Time Series (Daily)"
      - think: 2

      - log: "Testing with invalid symbol"
      - get:
          url: "/query"
          qs:
            symbol: "INVALID_SYMBOL"
            function: "{{ function }}"
            apikey: "{{ $processEnvironment.API_KEY }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.iopu_AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with invalid function"
      - get:
          url: "/query"
          qs:
            symbol: "{{ symbol }}"
            function: "INVALID_FUNCTION"
            apikey: "{{ $processEnvironment.API_KEY }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.iopu_AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with invalid API key"
      - get:
          url: "/query"
          qs:
            symbol: "{{ symbol }}"
            function: "{{ function }}"
            apikey: "INVALID_API_KEY"
          headers:
            Authorization: "Bearer {{ $processEnvironment.iopu_AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with missing required parameters"
      - get:
          url: "/query"
          qs:
            apikey: "{{ $processEnvironment.API_KEY }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.iopu_AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with invalid URL"
      - get:
          url: "/invalid_endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.iopu_AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
